# libft_project_42
Libft is the foundational C library project at 42 School, requiring students to manually reimplement standard libc functions (e.g., strlen, memcpy) and create custom utility functions (ft_split, ft_strjoin, linked lists). It serves as a personal, reusable toolbox for future C projects, enhancing understanding of data structures,
